services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-chat-django
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data:
